/* istanbul ignore file */
import styled from "styled-components";

const Body = styled.div`
  @media (max-width: 1000px) {
    padding-left: 5px;
    padding-right: 5px;
  }

  min-height: 700px;
  padding-top: 100px;
`;

export default Body;
